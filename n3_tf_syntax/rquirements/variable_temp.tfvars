# when we have more than one terraform.tfvars in our directory then during terraform apply
# we must explicitly write in the terminal such as:
# terraform apply -var-file=variable_temp.tfvars

instance_name = "hello-world-2"